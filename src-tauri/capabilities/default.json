{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Project folder selection and read-only inspection",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
